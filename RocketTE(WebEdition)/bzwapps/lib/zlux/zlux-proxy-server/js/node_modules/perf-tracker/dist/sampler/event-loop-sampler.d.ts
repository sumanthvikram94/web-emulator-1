/// <reference types="node" />
import { ISampler } from './sampler';
import { EventLoopUtilization } from 'perf_hooks';
export declare class EventLoopSampler implements ISampler {
    sampleFunc: () => void;
    sampleData: EventLoopUtilization | undefined;
    private _lastElu;
    constructor();
    onSample(callback: Function): void;
    isAvailable(): boolean;
}
