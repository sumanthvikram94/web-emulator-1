export declare class HourSummary {
    peaks: number[];
    avgs: number[];
    hour: number;
    constructor(hour: number);
    setPeak(minute: number, peak: number): void;
    setAvg(minute: number, avg: number): void;
    getLength(): number;
}
