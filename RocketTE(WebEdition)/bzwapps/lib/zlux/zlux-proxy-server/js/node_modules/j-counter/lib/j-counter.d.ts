import { Counter } from './counter';
import { Summarizer } from './summarizer';
export declare class JCounter {
    private counters;
    private summarizers;
    private DEFAULT_NAME;
    private defaultCounter;
    constructor();
    getCounter(name?: string): Counter;
    createCounter(name: string): Counter | undefined;
    getLastCount(name?: string): number;
    addOne(name?: string): boolean;
    add(pace: number, name?: string): boolean;
    dropOne(name?: string): void;
    drop(pace: number, name?: string): boolean;
    getCountAt(ts: number, name?: string): number;
    getLastAt(): number;
    getPeak(begin?: number, end?: number, name?: string): number;
    getAvg(begin?: number, end?: number, name?: string): number;
    cut(cutAt?: number, name?: string): {
        cutAt: number;
        cutKeys?: undefined;
        cutValues?: undefined;
    } | {
        cutAt: number;
        cutKeys: number[];
        cutValues: number[];
    };
    triggerSummarizer(interval?: number, name?: string): Summarizer;
    getSummarizer(name?: string): Summarizer;
    getMinutePeak(name?: string): {
        hour: number;
        peaks: number[];
    }[];
    getMinuteAvg(name?: string): {
        hour: number;
        avgs: number[];
    }[];
    getHourPeak(name?: string): {
        hour: number;
        peak: number;
    }[];
    getHourAvg(name?: string): {
        hour: number;
        avg: number;
    }[];
    deleteCounter(name: string): void;
    destroyAll(): void;
    pause(name?: string): void;
    resume(name?: string): void;
}
