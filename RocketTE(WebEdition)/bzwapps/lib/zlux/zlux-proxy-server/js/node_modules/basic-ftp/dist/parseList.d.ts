import { FTPContext } from "./FtpContext";
/**
 * Parse raw directory listing.
 */
export declare function parseList(rawList: string, serverType: number, JES: number, hasMLSD: boolean, ftp: FTPContext): any[];
