/**
 * Returns true if a given line might be a MVS-style listing.
 *
  */
export declare function testLine(line: string): boolean;
/**
 * Parse a single line of a MVS-style directory listing.
 */
export declare function parseLine(line: string): any | undefined;
export declare function transformList(files: any[]): any[];
