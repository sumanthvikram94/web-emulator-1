const waitTime = (ms = 5000) => {
    return new Promise((resolve) => {
        // console.log(`Waiting for ${ms} ms...`)
        setTimeout(() => {
            resolve(true)
        }, ms);
    })
}

export { waitTime }
