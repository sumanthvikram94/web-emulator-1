/// <reference types="node" />
import EventEmitter from 'events';
export declare class FileWatcher extends EventEmitter {
    private _dir;
    private _dir_stats;
    private _prom;
    private _timeout;
    private _closed;
    private _fileService;
    private _isFirst;
    constructor();
    watchDir(dir: string, options: any): void;
    static watch(dir: string, options: any): any;
    close(): void;
}
