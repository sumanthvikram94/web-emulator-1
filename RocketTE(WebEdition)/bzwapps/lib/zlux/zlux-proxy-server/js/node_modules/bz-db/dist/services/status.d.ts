declare class Status {
    _status: string;
    _promise: Promise<any>;
    _resolver: Function;
    _promiseStatus: Promise<any>;
    _resolverStatus: Function;
    _ready: boolean;
    _timing: number;
    _observer: any;
    constructor();
    setObserver(observer: any): void;
    setUnresolvedPromise(): void;
    resolvePromise(): void;
    resolveStatusPromise(): void;
    get status(): string;
    set status(s: string);
    markLoadReady(): void;
    waitStatusReady(): Promise<string>;
    waitLoadReady(): Promise<string>;
    get ready(): boolean;
}
export { Status };
