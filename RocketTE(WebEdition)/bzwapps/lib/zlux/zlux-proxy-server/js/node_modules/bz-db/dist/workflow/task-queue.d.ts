import Queue from 'better-queue';
import { Task } from './task.js';
interface QueueOptions {
    task: Task;
}
declare class TaskQueue {
    _q: Queue;
    constructor();
    push(task: Task): Promise<any>;
}
export { TaskQueue, QueueOptions };
